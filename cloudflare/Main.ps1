param (
    [Parameter(Mandatory = $true)]
    [string]$ApiToken,

    [Parameter(Mandatory = $true)]
    [string]$ZoneName
)

# Get Zone Id

# Get DNS records in format HashMap < @{ recordName, id } >

