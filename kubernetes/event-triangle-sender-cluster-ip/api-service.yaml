---
kind: Service
apiVersion: v1
metadata:
  name: sender-service
spec:
  selector:
    app: event-triangle-sender-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
