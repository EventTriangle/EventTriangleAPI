---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-api-deployment
spec:
  selector:
    matchLabels:
      app: auth-api
  replicas: 3
  template:
    metadata:
      labels:
        app: auth-api
    spec:
      containers:
        - name: auth-api
          image: kaminome/auth-service:0.3.0-alpha.235
          ports:
            - containerPort: 80
          env:
            - name: EVENT_TRIANGLE_AD_CLIENT_SECRET
              valueFrom:
                configMapKeyRef:
                  name: auth-configmap
                  key: EVENT_TRIANGLE_AD_CLIENT_SECRET

            - name: DatabaseConnectionString
              valueFrom:
                configMapKeyRef:
                  name: auth-configmap
                  key: DatabaseConnectionString