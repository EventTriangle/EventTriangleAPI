apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Release.Name }}-service
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: {{ .Release.Name }}-service
